<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: ListPreference's summary should be set to its value by the activity code.-->
    <ListPreference
        android:defaultValue="@string/pref_default_synth_mode"
        android:entries="@array/synth_mode_entries"
        android:entryValues="@array/synth_mode_values"
        android:key="synthMode"
        android:title="@string/pref_title_synth_mode" />

</PreferenceScreen>
